# Chococo frontend

React 19 + TypeScript (Vite) 製のフロントエンド。現状はモックデータのみで画面遷移を確認できるプロトタイプ（S1〜S7、API通信・認証は未実装）。詳細は[プロジェクトルートのREADME](../README.md)・[設計ドキュメント](../docs/)を参照。

## 起動

```bash
npm install
npm run dev
```

`http://localhost:5173` で起動する（ポート固定。[技術スタック](../docs/tech-stack.md)参照）。
